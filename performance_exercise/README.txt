To  execute the program:
- From this directory (where the 'anagrams' file is put (the extension jar is removed so it can be sent by email)), run this command line:
java -jar anagrams resources/sample.txt
